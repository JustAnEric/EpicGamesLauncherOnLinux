import tkinter, pywebview
from tkinter import Tk

window = Tk()

label = tkinter.Label(label="Please wait while we edit the configuration in ./home/user/.cache/epicgames/make.properties")
label.pack(window)

window.mainloop()
